import React, { FC } from 'react';
import { Layout, SEO } from '../components';

const IndexPage: FC = () => {
  return (
    <Layout>
      <SEO />
      Hello World!
    </Layout>
  );
};

export default IndexPage;
