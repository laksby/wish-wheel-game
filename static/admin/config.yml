backend:
  name: git-gateway
  branch: main

media_folder: static/img
public_folder: /img

collections:
  - name: 'data'
    label: 'Data'
    folder: 'data'
    create: true
    slug: '{{slug}}'
    editor:
      preview: false
    fields:
      - label: 'Title'
        name: 'title'
        widget: 'string'
      - label: 'Type'
        name: 'type'
        widget: 'select'
        options: ['prize', 'champagne', 'cocktail']
